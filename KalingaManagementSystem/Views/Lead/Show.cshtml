@model IEnumerable<KalingaManagementSystem.Models.Lead>

@{
    ViewBag.Title = "Show";
}

<h2>Show</h2>
<div>
    @ViewBag.message
</div>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LeadID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.ContactNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CatogeryId)
        </th>*@
        <th style="padding-left:50px"> Actions  </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.LeadID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Name)
    </td>
    @*<td>
        @Html.DisplayFor(modelItem => item.ContactNumber)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.CatogeryId)
    </td>*@
    <td>
        @*@Html.ActionLink("Edit", "Edit", new { id = item.LeadID }) |*@
        @Html.ActionLink("Students Uder Lead", "Details", new { id = item.LeadID }) 
        @*@Html.ActionLink("Delete", "Delete", new { id = item.LeadID })*@
    </td>
</tr>
}

</table>

